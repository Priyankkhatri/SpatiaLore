PORT=3001
ALLOWED_ORIGIN=http://localhost:5173

# Primary: self-hosted model
LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=not-needed-for-local-but-required-by-sdk
LLM_MODEL_NAME=llama-3-8b-instruct

# Fallback: Groq free tier
GROQ_API_KEY=
GROQ_MODEL_NAME=llama-3.1-8b-instant

# Supabase URL for analytics insertion
SUPABASE_URL=https://lvodmlfhbchogmkdgooy.supabase.co

# CRITICAL: SUPABASE_SERVICE_ROLE_KEY bypasses ALL row-level security.
# This must NEVER be committed to git, never logged, never sent in any API response,
# and never used in spatialore-dashboard or spatialore-mobile. It exists ONLY in this backend's server environment.
SUPABASE_SERVICE_ROLE_KEY=
